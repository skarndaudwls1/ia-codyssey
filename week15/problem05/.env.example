# mars_weather_summary.py 가 읽는 DB 접속 정보 예시.
# 이 파일을 복사해 .env 로 만들고 실제 값을 채운다.
#   cp .env.example .env
# .env 는 깃에 올리지 않는다(.gitignore 에 등록됨).
MYSQL_HOST=127.0.0.1
MYSQL_PORT=3306
MYSQL_USER=mars
MYSQL_PASSWORD=
MYSQL_DATABASE=mars_db
